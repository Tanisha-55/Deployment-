{
  "name": "copilot-lineage-deriver",
  "displayName": "Copilot Lineage Deriver",
  "description": "Generate lineage CSV files from XML using Copilot agent mode with @extract-lineage command",
  "version": "1.0.0",
  "publisher": "your-publisher-name",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:copilot-lineage-deriver.generateLineage",
    "onChatParticipant:extract-lineage"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilot-lineage-deriver.generateLineage",
        "title": "Generate Lineage CSV from XML"
      },
      {
        "command": "copilot-lineage-deriver.extractLineageSingle",
        "title": "Extract Lineage for Current XML File",
        "category": "Copilot Lineage"
      }
    ],
    "configuration": {
      "title": "Copilot Lineage Deriver",
      "properties": {
        "copilotLineageDeriver.mappingsFolderPath": {
          "type": "string",
          "default": "individual_mappings",
          "description": "Relative path to the folder containing mapping subfolders with XML files"
        },
        "copilotLineageDeriver.instructionsPath": {
          "type": "string",
          "default": "instructions.md",
          "description": "Relative path to the instructions markdown file"
        },
        "copilotLineageDeriver.outputFolder": {
          "type": "string",
          "default": "lineage_csv",
          "description": "Relative path for the output CSV files"
        },
        "copilotLineageDeriver.delayBetweenRequests": {
          "type": "number",
          "default": 2000,
          "description": "Delay between Copilot requests in milliseconds"
        },
        "copilotLineageDeriver.batchSize": {
          "type": "number",
          "default": 5,
          "description": "Number of files to process in each batch"
        },
        "copilotLineageDeriver.enableBatchProcessing": {
          "type": "boolean",
          "default": true,
          "description": "Enable batch processing for large numbers of files"
        }
      }
    },
    "chatParticipants": [
      {
        "id": "extract-lineage",
        "name": "Extract Lineage",
        "description": "Extract lineage from XML files using Copilot",
        "fullName": "Lineage Extractor",
        "icon": "$(file-code)",
        "when": "resourceExtname == .xml"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}

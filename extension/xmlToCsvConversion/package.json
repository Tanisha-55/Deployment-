{
  "name": "copilot-lineage-deriver",
  "displayName": "Copilot Lineage Deriver",
  "description": "Generate lineage CSV files from XML using Copilot agent mode",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:copilot-lineage-deriver.generateLineage"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilot-lineage-deriver.generateLineage",
        "title": "Generate Lineage CSV from XML"
      }
    ],
    "configuration": {
      "title": "Copilot Lineage Deriver",
      "properties": {
        "copilotLineageDeriver.xmlFolderPath": {
          "type": "string",
          "default": "informatica",
          "description": "Relative path to the folder containing XML files"
        },
        "copilotLineageDeriver.instructionsPath": {
          "type": "string",
          "default": "instructions.md",
          "description": "Relative path to the instructions markdown file"
        },
        "copilotLineageDeriver.outputFolder": {
          "type": "string",
          "default": "output",
          "description": "Relative path for the output CSV files"
        },
        "copilotLineageDeriver.maxConcurrent": {
          "type": "number",
          "default": 5,
          "description": "Maximum number of concurrent Copilot requests"
        },
        "copilotLineageDeriver.delayBetweenRequests": {
          "type": "number",
          "default": 1000,
          "description": "Delay between Copilot requests in milliseconds"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}
